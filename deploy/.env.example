# Static Vite/Nginx deployment does not need runtime secrets.
# Keep the server file at /app/chinese-magic/.env, even if it is empty,
# so docker compose can load it consistently.
APP_BIND=0.0.0.0
APP_PORT=18080
